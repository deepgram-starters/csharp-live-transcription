# Required
DEEPGRAM_API_KEY=%api_key%

# Optional (defaults shown)
PORT=8081
HOST=0.0.0.0
FRONTEND_PORT=8080

# Session auth (optional; use at least 32 UTF-8 bytes when set)
# SESSION_SECRET=%session_secret%
